## Update landing page to match reference screenshot

Edit `src/routes/index.tsx` only.

### Header

- Change logo: `Latino` in dark green `#1A5336` + `NZ` in gold `#EFC64E` (bold, no green square thumbnail/box).
- Remove`Entrar` button

### Hero section

- Wrap hero in a dark green block: `bg-[#0F3D24]` (deep green) with subtle radial gradient, white text, rounded-3xl with horizontal padding.
- Small pill badge centered: `● COMUNIDADE LATINA NA NZ` (dark translucent bg, gold dot, white uppercase text).
- Headline: `Encontre negócios` on line 1, `latinos na Nova Zelândia` on line 2 — with the word `latinos` in gold `#EFC64E`, rest in white. Large, black weight, tight tracking.
- Subheadline (white/70): `O diretório moderno de profissionais e empresas latinas. De autônomos a grandes negócios, tudo em um só lugar.`
  &nbsp;

### Features + footer

- Keep existing 3 feature cards and footer below the hero (on white bg).



### Out of scope

No backend, modal, or routing changes.